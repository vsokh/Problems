#include <vector>
#include <iostream>
#include <set>
#include <math.h>
#include <algorithm>
#include <iostream>

using namespace std;

/* #define rep(i,a,b) for(int i=a;i<b;i++) */

set<int> st;

void solve()
{
	int n = 0;
	/* rep(i, 1, 200) { */
	/* 	st.insert(i*i); */
	/* } */
	/* auto print = [](const int& n) { std::cout << " " << n; }; }; */
	/* cout << "HI"<<endl; */
}

int main()
{
	int tests = 0;
	cin >> tests;
	/* while (tests--) */
	solve();
	return 0;
}

